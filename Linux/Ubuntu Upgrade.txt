#if it’s not installed.
sudo apt install update-manager-core

#update system software 
sudo apt update
sudo apt dist-upgrade

#edit /etc/update-manager/release-upgrades file and set the variable prompt as below
# Prompt=normal
# never - no upgrade, lts - only lts releases, normal - all releases

#Start the upgrade tool for last stable version
sudo do-release-upgrade

#Or start the upgrade tool for development version
#sudo do-release-upgrade -d